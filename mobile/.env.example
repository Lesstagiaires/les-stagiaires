# URL de l'API — préfixe EXPO_PUBLIC_ obligatoire pour être lue côté client (Expo).
# En dev, remplacer localhost par l'IP locale de la machine si testé sur un appareil
# physique via Expo Go (localhost y désigne l'appareil, pas la machine de dev).
EXPO_PUBLIC_API_URL="http://localhost:3000"
